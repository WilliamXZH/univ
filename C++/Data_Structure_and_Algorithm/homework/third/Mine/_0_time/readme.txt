作业说明：
1.链表使用类作为链节，而不是结构体
2.压缩文件包含一个链表头文件（使用模板类），一个测试文件（本来想写菜单的。。。但感觉太麻烦了，就没有使用），以及此文件
3.编译器使用editpuls配置的VC++ 6.0，只是为了写代码快一点，实际编译效果和VC一样的
4.链表共试写了三次，提交的为最终版，头文件LinkList.h，以及测试文件test.cpp，之前的或是未用模板。。或是因函数过多，导致代码过于冗杂，而难以实现所有功能
5.总是想按照Java的方式来写。。导致一些函数难以实现，所以最后干脆将private里的数据写在public里直接使用。。。
