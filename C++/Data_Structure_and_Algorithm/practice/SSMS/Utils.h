#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <iostream>
using namespace std;

int pow(int x,int y);
int parseInt(string mes);
int parseChar(string mes);

/* Not concrete. */
string * split(string mes,string separator);
#endif

